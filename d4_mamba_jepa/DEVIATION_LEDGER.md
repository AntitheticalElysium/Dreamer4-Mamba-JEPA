# Deviation ledger

Every departure from a pinned primary implementation must be entered here
before training. A change is not allowed to hide behind a generic "D4-Mamba"
label.

Status values:

- `PLANNED`: documented, not implemented.
- `IMPLEMENTED`: code and unit tests exist.
- `SCREENED`: smallest discriminating runtime check passed.
- `REJECTED`: implementation retained only as evidence or removed.

| ID | Status | Local change | Source contract retained | Why required | Isolation / first failure suspect |
|---|---|---|---|---|---|
| D000 | SCREENED | Load MMBench2 `model.py` by exact path and reject digest drift | File contents and class definitions remain untouched | Its executable uses flat imports and is not packaged as a dependency | Source-loader test passes; a digest failure means source drift, not a model failure |
| D001 | SCREENED | Reduce image resolution and model width/depth to a named `D4LiteConfig` | Same tokenizer, block-causal layers, shortcut tokens, and flow head | Upstream training scale requires hardware far beyond 6 GB | Four-arm CUDA smoke, synthetic overfit, and real T-BASE preflight pass; remaining failure may still be under-capacity. GEOMETRY VERIFIED 2026-07-27 against the pinned Dreamer 4 appendix (`edwhu__dreamer4-jax/docs/appendix.txt:14`): the paper uses `N_b=512` latent tokens x `D_b=16` channels, reshaped to `N_z=256 x 32` for the dynamics, from 960 patch tokens (384x640, patch 16). Ours is `n_latents=16` x `d_bottleneck=16` packed to `n_spatial=4` x 64, from 64 patch tokens (64x64, patch 8). `d_bottleneck` therefore MATCHES the paper; `n_latents` is 32x below and spatial tokens into the dynamics 64x below. Note the ratios do not all point the same way: relative to input, we retain 2.08% of the frame's floats against the paper's 1.11%, and 0.25 latents per patch against 0.53, so we are ~2x more compressed per patch but not more compressed in absolute retained fraction. The `d_bottleneck` 16/32/64 ladder (20k updates, all else pinned) leaves oracle preservation unchanged (0/1/1 of 16 continuous targets) with non-monotonic dev cosine (0.731/0.795/0.702), so channel width is not what discards the targets. `n_latents` is under test; no value is rejected |
| D002 | SCREENED | Replace the continuous 16-D action MLP with a 17-way discrete embedding | Exactly one action token occupies the same token-layout slot | Crafter exposes 17 categorical actions | Led-to/indexing tests pass; after 5k T-BASE updates, paired action shuffling raises uniform-dev flow loss by 21.6% |
| D003 | SCREENED | Add a continuation MTP head beside the upstream reward MTP head | Head reads the same post-transition agent tokens | Crafter planning must distinguish terminal from continuing futures | Alignment and gradient tests pass, but efficacy fails: the 5k baseline predicts P(continue)=.998 on terminal-aligned generated states |
| D004 | SCREENED | Replace only `TimeSelfAttention` modules in dynamics with official Mamba-2 modules | Spatial attention, MLP, residual ordering, token layout, tokenizer, and heads stay unchanged | Tests the original long-context temporal-compute hypothesis | Source/mechanical checks pass; matched real-Crafter M1 passes all frozen feasibility gates and reproduces bit-exactly under the pinned official deterministic path; there is no short-context speed win |
| D005 | SCREENED | Branch a clean Mamba prefix state across shortcut denoising candidates and commit only the generated latent | Each denoising candidate sees one identical clean prefix | Mamba state is mutable whereas attention KV context is functionally shared | Cached/uncached equivalence and candidate-isolation tests pass; first suspect for any future rollout-only Mamba failure |
| D006 | SCREENED | Add a CDP-shaped cosine predictor from clean causal agent state plus next action to a stop-gradient future latent; update the encoder slowly and retain a frozen-decoder reconstruction anchor | Upstream flow loss remains present, receives detached tokenizer latents, and stays independently reportable | Tests predictive representation learning without letting future-target gradients or a moving decoder obscure attribution | Target-detach, encoder-gradient, decoder-freeze, optimizer-group, and CUDA tests pass; scientific efficacy remains untested |
| D007 | SCREENED | Reuse episode-bounded replay and canonical Crafter wrapping from `m3_hjwm_compact` through adapters | Observation/action/reward/continuation timing is unchanged | These utilities already passed indexing and reproducibility regressions | Adapter/source/replay hashes pass; real-data training and two bit-identical executed trajectories pass |
| D008 | SCREENED | Use categorical random shooting before implementing categorical CEM or PMPO | Upstream imagined reward scoring and first-action receding horizon remain | Produces executed behavior early with fewer new optimizer assumptions | Three-seed executed preflight and exact substantive repeat pass; it remains an evaluation instrument, not a learned Dreamer policy |
| D009 | SCREENED | Add strict atomic checkpoints around the composed model, full config, source digests, optimizer, and every RNG | Model state still loads with `strict=True`; no source checkpoint is rewritten | The upstream executable checkpoint format cannot reconstruct local adapters and prior work exposed partial RNG resume | Strict roundtrip, digest/config/source rejection, Torch/NumPy resume, tokenizer roundtrip, and failed-save preservation tests pass |
| D010 | SCREENED | Port only the tokenizer reconstruction, shortcut-flow, reward-alignment, and rollout equations needed around the unchanged upstream model classes | Operators, led-to convention, RMS normalization, K-grid, and Euler update follow the pinned scripts | MMBench2 is an executable-style project whose flat trainers cannot be safely imported as a library | Line-level provenance is in docstrings; synthetic overfit, paired action shuffle, cached rollout, and real preflight are the first parity checks |
| D011 | SCREENED | Add official Gymnasium `CartPole-v1` as a small control baseline with action repeat 2 and a three-channel pixel adapter: full foreground, pixel-localized zoom, and frame difference | Official dynamics, reward, termination, time limit, and RGB renderer are untouched; every derived view uses rendered pixels only | At 64×64, the raw mostly-white render let the reconstruction tokenizer ignore pole angle and action effects; the adapter makes task pixels measurable without exposing simulator state to the model | Source and installed-file hashes pass; held-out tokenizer probes recover cart position and pole angle; this task-specific observation adapter is the first suspect if transfer to raw imagery fails |
| D012 | SCREENED | Route reward and continuation heads through the agent tokens from the same noised shortcut-flow forward pass | This is the exact MMBench2 `dynamics_pretrain_loss` task-head route | The initial local port incorrectly made a second clean-latent task-head pass, creating a train/deployment mismatch | A regression forbids the second clean pass; the corrected world obtains an 18.2% paired action-shuffle penalty and positive executed planning |
| D013 | SCREENED | For the two-action control check, optionally enumerate all 256 horizon-8 plans and use common random numbers across candidates | Denoising schedule, imagined reward/continuation scoring, and first-action receding horizon are unchanged | Removes finite random-plan coverage and candidate-noise confounds from the smallest deterministic benchmark | On 30 fresh seeds, the frozen imagined planner scores 44.87 versus 19.00 random, paired delta +25.87 with 95% bootstrap CI [15.80, 36.73]; it misses the separately declared absolute mean-50 gate |
| D014 | SCREENED | Add a categorical version of the MMBench2 attention-pooled, gradient-isolated BC policy head and train it only on demonstration actions | Agent-token input, attention pooling, MLP shape, small output initialization, and head-only BC gradient boundary follow upstream `PolicyHeadMTP` | A source-shaped policy gives a positive executed baseline before Mamba or JEPA experiments and separates world competence from planner quality | Held-out action accuracy is 87.04%; on 30 fresh seeds the frozen pixel policy scores 288.70 versus 17.60 random, wins 30/30, and reproduces exactly |
| D015 | SCREENED | Serialize checkpoints through a binary file handle before atomic replacement | Payload, strict loading, and atomic replace semantics are unchanged | PyTorch otherwise embeds a random temporary basename in its ZIP archive | Identical model state now yields identical checkpoint bytes; policy weights reproduce bit-for-bit |
| D016 | SCREENED | Add the Dreamer-4 imagination phase as an actor/value-only update: initialize the actor from the exact BC head, freeze an exact BC prior, freeze the tokenizer/world/reward/continuation modules, train a zero-output symexp-twohot value head with TD-lambda targets, and train the actor with balanced PMPO plus reverse `KL(actor || BC)` | Dreamer 4 equations 10-11, `gamma=0.997`, `alpha=0.5`, `beta=0.3`, one rollout per replay context, direct policy execution, and the paper's frozen-transformer contract are retained | This is the missing phase that turns the working world-plus-BC stack into an imagination-trained agent instead of a shooting controller | Twelve pre-training regressions establish indexing, loss direction, exact initialization, gradient isolation, frozen tensors, checkpoint pairing/source rejection, and absence of planner routing; the selected actor clears executed BC parity on 100 fresh seeds |
| D017 | SCREENED | Use one CartPole task, the existing distance-zero categorical BC output, 32 imagined decisions, a sliding clean context of 8, and the existing pixel adapter instead of Minecraft task tokens, 192-frame context, `tau_ctx=0.1` corruption, and eight BC MTP distances | RL still acts from the final causal agent tokens, samples one action per state, rolls the source-pinned shortcut world, and applies the Dreamer-4 actor/value losses only to imagined data | Task embeddings are vacuous for one task; only distance zero is consumed by RL; adding unseen context corruption would mismatch the trained local world; the reduced benchmark cannot reproduce Minecraft scale | These scale/interface divergences run end to end, but remain first suspects for transfer; the selected local actor scores 281.33 versus 249.32 paired BC |
| D018 | SCREENED | Use a restarted Adam at `1e-4`, global norm clipping at 1.0, and symlog centers `[-10,10]` for the value head | Adam `1e-4` and a categorical symlog value head follow the inspected Dreamer-4 JAX reproduction; zero output initialization follows DreamerV3 and the Dreamer-4 paper | Dreamer 4 does not publish all optimizer/value-bin details, while the exact large-scale optimizer state is unavailable | Training remains finite and reproduces tensor/history exact; held-out value CE is 3.136, but value MAE 24.0 and imagined continuation near one remain explicit limitations. Sourcing verified 2026-07-23: `bins: 255` and the zero-initialized output (`outscale: 0.0`) on a `symexp_twohot` value head are read directly from `danijar/dreamerv3` `dreamerv3/configs.yaml:101`, which Dreamer 4 inherits; the symlog centre range [-10,10] remains a local choice |
| D019 | REJECTED | Select actor checkpoints only on a reserved development seed set and open a separate 30-seed parity set once; the first fixed budget ladder used batch 16 and 100, 250, 500, then 1,000 updates | Direct greedy actor execution without shooting or online learning matches the frozen policy deployment contract | The small benchmark needs a falsifiable completion rule and protection against repeated final-set tuning | The selected 1,000-step actor passed development mean parity but failed sealed parity: 208.60 versus 242.60 BC, paired -34.00 with CI [-71.87, 3.77]. The additional 288.70 cross-seed threshold was also invalid as a parity rule because the paired BC itself varied, but removing it cannot rescue this rejection |
| D020 | SCREENED | Increase the actor/value batch from 16 to the inspected Dreamer-4 reproduction's 64 distinct contexts while keeping one rollout per context and every algorithmic setting fixed; reserve new final seeds and make historical BC performance descriptive only | Dreamer 4's one-rollout-per-context diversity rule, PMPO equation, BC prior, and frozen-transformer boundary remain exact; the inspected actor runner uses `B=64` | The original 6 GB constraint assumption was false after measured peak use of only 63 MB, and small-batch PMPO noise moved the actor despite nearly action-constant imagined reward/continuation | Batch-64/250 updates reaches 281.33 versus 249.32 paired BC on fixed `980000:980100`, delta +32.01 [6.66,57.94]; retraining and full executed evaluation reproduce exactly |
| D030 | SCREENED | Add a genuinely non-generative `representation_objective="jepa"` arm (`T-JEPA`): the deterministic action-conditioned next-embedding predictor (`predict_next_jepa`/`_sample_next_jepa`) is the entire rollout, with no denoising in `sample_next_packed`; remove the shortcut-flow denoiser objective, the pixel decoder, MAE masking, and the reconstruction anchor | Pixel adapter, spatial attention, temporal operator (Transformer `TimeSelfAttention`), reward/continuation MTP heads, actor/value, BC prior, checkpoint format, and the sealed evaluation protocol are held identical to the screened T-BASE actor-critic | This is the distinct non-generative arm the CDP arm (D006) is not: CDP keeps the generative flow as the rollout and bolts a cosine loss beside it; `T-JEPA` deletes generation entirely | Construction/loss/rollout smoke passes; 59 base/cdp tests still pass; first suspect is representation collapse (monitored via online-std) and whether a deterministic single-pass predictor carries enough control signal |
| D031 | SCREENED | Anti-collapse by faithful SPR/BYOL EMA-target self-prediction: a stop-gradient EMA target encoder (`copy.deepcopy`, `requires_grad_(False)`) on the actual next frame, an asymmetric online projection+prediction vs EMA target projection, and an L2-normalized MSE (= negative cosine) loss | Ports `mila-iqia/spr` `0b9dd4e7` `do_spr_loss`/`spr_loss` (`F.normalize` then MSE) and the I-JEPA `52c1ae95` / V-JEPA-2 `204698b4` linear target-encoder momentum ramp; not hand-rolled or hybridized | The user requires the non-generative arm's anti-collapse to be a source-faithful SPR-style EMA target, and the imagined return to become policy-dependent | Stop-grad verified (target encoder receives no gradient; online encoder does); EMA update moves the target; first suspect is momentum schedule (`tau` 0.99->0.999) and projection batch-norm behaviour at small batch |
| D032 | SCREENED | Train the `T-JEPA` world in one joint phase (`train_jepa_world`): online encoder + dynamics + predictor + task heads under a single AdamW, with the EMA target updated after every `optimizer.step()`; there is no separate tokenizer MAE pretraining phase | CORRECTED 2026-07-23: this row previously claimed the heads' clean dynamics pass "matches deployment exactly" and that "the D012 noised-pass requirement was flow-specific". Both claims were FALSE. Training read real encoder latents while `rollout._sample_next_jepa` feeds the heads predictor-generated latents, so the arm had exactly the D012 train/deployment mismatch, not an exemption from it. D043 fixes the routing. Batch, sequence length, warmup, clip and terminal fraction do follow the T-BASE world phase | The encoder can no longer be pretrained by reconstruction (no decoder), so it must learn jointly from the self-prediction signal, as in SPR | Finite loss and gradients in smoke; first suspect is joint-training stability and whether the reward/continuation grounding is enough to keep the online latent task-relevant. DISCLOSED 2026-07-27, CORRECTED 2026-07-28. The closest action-conditioned baseline is `facebookresearch/vjepa2` V-JEPA 2-AC (`configs/train/vitg16/droid-256px-8f.yaml`, `app/vjepa_droid/train.py`). CORRECTION: the 2026-07-27 note claimed it "also trains its encoder (full LR by default)" because the encoder appears in `init_opt`'s param groups. That was WRONG. In the training step the encoder is ABSENT FROM THE LOSS GRAPH: `forward_target` computes `h = target_encoder(c)` under `torch.no_grad()` (`train.py:408-415`), `forward_predictions` runs only the predictor, and the loss is a function of predictor outputs against that detached `h`. `grep "encoder("` over the file returns exactly one call, the no-grad `target_encoder`. V-JEPA 2-AC therefore EFFECTIVELY FREEZES its pretrained encoder and trains the predictor. Dreamer 4 likewise freezes its pretrained tokenizer during dynamics learning. It also uses no bottleneck (full patch-token grid at 256px/patch 16/tubelet 2/8 frames). Our arm jointly trains a RANDOM encoder at full LR through a 4-token bottleneck, which both reference systems avoid in two independent ways. SPR is not a standalone precedent either: `mila-iqia/spr` `src/algos.py:131` optimizes `loss + model_rl_weight*model_rl_loss + reward_loss_weight*reward_loss + spr_loss` jointly, so its encoder is anchored by Q-learning and reward, not by SPR alone. Dreamer-CDP separates encoder and dynamics learning rates by 66.7x (`enc_lr: 6e-6` vs `dyn_lr: 4e-4`, `dreamerv3/configs.yaml:85`). Unanchored full-LR encoder training is therefore a divergence from every pinned reference; untested here |
| D033 | SCREENED | Use BatchNorm1d projection/prediction MLPs (SPR/BYOL `global_classifier` shape) applied over the folded `[B*T]` batch, and deliberately do NOT insert SPR's `renormalize` min-max step into the shared packed-latent path | Projection MLP matches SPR `Linear->BN->ReLU->Linear`; the packed-latent interface into the dynamics stays byte-identical to T-BASE so the temporal operator is unchanged | SPR's anti-collapse relies on batch-norm in the projector; inserting `renormalize` into the latent would diverge the tokenizer/dynamics interface from the T-BASE control and confound the objective comparison | BN provides the SPR normalization off the shared path; first suspect if collapse appears is that BN over `[B*T]` at the training batch size is too weak |
| D034 | SCREENED | Correct the self-prediction to SPR's multi-step form: apply the predictor autoregressively for `jepa_jumps=5` steps, feeding its own output back and matching each step to the EMA target of the actual future frame (was a single teacher-forced step) | Faithful to `mila-iqia/spr` `0b9dd4e7` `do_spr_loss` `jumps`; the predictor, EMA target, and loss form are unchanged, only the number of autoregressive steps | The initial single-step port trained teacher-forced but DEPLOYED autoregressively, so imagined multi-step rollouts drifted off the training manifold and never reached terminal states (imagined continue stayed 1.0). SPR's whole point is multi-step | Direct evidence: under the corrected world, imagined continuation under anti-BC drops to min 0.12 (was exactly 1.0), and imagined discounted return becomes policy-dependent (BC 59.2 vs anti-BC 54.7); peak VRAM 275 MB, training stable |
| D035 | SCREENED | Terminal-class-weighted continuation loss (`jepa_terminal_weight=8`, `jepa_terminal_fraction=0.5`): up-weight the rare `continue==0` targets and oversample terminal windows so the head does not collapse to the majority label | `continuation_mtp_loss` gains an optional `terminal_weight` (default 1.0, so base/cdp are byte-identical); the head architecture and MTP routing are unchanged | A frozen-world agent-token linear probe shows the terminal signal IS present in the JEPA latent (terminal-within-5 AUC 0.869 vs T-BASE 0.712), but the unweighted head collapsed to a constant (real-state continue std 0.0) on CartPole's sparse terminals | After weighting, real-state continue dispersion recovers (std 0.22, min 0.0, max 1.0); combined with D034 this makes imagination beat BC on the sealed tier |
| D043 | SCREENED | Route the JEPA reward and continuation heads through the post-transition agent tokens of the imagined multi-step rollout, aligned to the real reward/continuation targets at the matching absolute positions, instead of a clean dynamics pass over real encoder latents. `jepa_self_prediction_loss` gains `return_rollout_agents`, which repeats the exact second dynamics pass `rollout._sample_next_jepa` performs at deployment | This restores the MMBench2 D012 contract for the non-generative arm: task heads are trained on the same forward pass they are read on at deployment. Head architecture, MTP alignment convention, terminal weighting (D035), predictor, EMA target and anti-collapse are unchanged | The heads are used ONLY inside imagination, where they read predictor-generated latents, but they were trained exclusively on real encoder latents. That is a train/deployment distribution mismatch of exactly the kind D012 was created to forbid, and D032 wrongly recorded it as an exemption. It plausibly confounds D040 (which read imagined-state continuation), D041 and the paused D042 | A regression asserts the training tokens are numerically identical to `sample_next_packed`'s deployment tokens for the first imagined step from the same context, so the two paths cannot silently diverge again. Cost is one extra dynamics pass per jump. Heads now see `jepa_jumps` positions per batch instead of `sequence_length`, so the first suspect is reduced head supervision, and the second is that only positions >= context receive any head gradient. RESULTS of the single post-fix retrain (world -> BC -> actor, development seeds only; the sealed tiers were NOT reopened and remain valid measurements of the pre-fix implementation). The defect was real and its removal improves the world on every measure: dev cosine 0.7617 versus 0.6750, final JEPA loss 0.1652 versus 0.2355, online std 0.1004 (no collapse), BC held-out accuracy 0.9112 versus 0.9105. It also corrects the imagination itself: imagined continuation rises 0.4438 -> 0.9888 and imagined return 6.25 -> 71.49, which is now consistent with a BC that really survives ~227 steps. This RETROSPECTIVELY UNDERMINES the D040 attribution: the low imagined continuation there was substantially an artifact of heads trained on real latents firing spuriously on predictor-generated ones, not solely rollout drift. But gate 1 does NOT move. On development seeds the actor scores 494.63 against BC 468.23, delta +26.40 [0.9,56.0], versus the pre-fix +29.33 [0.5,64.3] — both policies gain about 20-23 points and the gap is unchanged within overlapping intervals. Recorded as a valid negative result for gate 1: the head mismatch was a genuine defect worth fixing on its own merits, and it is not what was holding the actor-versus-BC margin down. If anything the ceiling problem tightens, since the actor now sits at 494.63 against a 500 cap |
| D044 | SCREENED | Use EMA target momentum ramped linearly 0.99 -> 0.999 over world steps (`jepa_ema_tau`, `jepa_ema_tau_final`) | Only the linear ramp FORM follows `facebookresearch/ijepa` `52c1ae95` `src/train.py:228`, `m = ema[0] + i*(ema[1]-ema[0])/N`. The endpoint VALUES are local | Registered after audit: D031 credited the I-JEPA momentum schedule without noting that I-JEPA's published endpoints are `ema: [0.996, 1.0]` (`configs/in1k_vith14_ep300.yaml`), not ours. I-JEPA ramps to exactly 1.0 over a 300-epoch ImageNet run; a 20k-step CartPole world reaching tau=1.0 would freeze the target completely, so the endpoints were shortened locally | The ramp form is source-exact and the values are now disclosed rather than implied. Untested against the published endpoints; first suspect if the target proves too fast (tau too low) or the representation stalls (tau too high) |
| D042 | PLANNED (PAUSED) | Select the `M-JEPA` imagination horizon on the reserved development seeds over a pre-declared ladder of 8/16/32, against the dev-selected `jepa_jumps=5` world. No world, BC, anti-collapse, head or optimizer setting changes; only the number of imagined decisions the actor and value are trained on moves | Actor-from-BC initialization, frozen world/prior/heads, PMPO equation 11, TD-lambda value, reverse-KL `beta`, one rollout per context and direct greedy execution are unchanged | This is the exact complement of D041. The world is trained on 5 autoregressive steps but the actor imagines 32, and D041 showed that raising the world's trained rollout to meet the actor produces a survivable horizon the actor then exploits (-122.53). The untried direction is lowering the actor's horizon to where the world is measurably accurate, giving short trustworthy signal instead of long exploitable signal. Like D038 and D041 the value was inherited from `T-JEPA` unexamined, so dev-seed selection is the established protocol | Selection is on development seeds only; sealed tiers are not consulted and the ladder runs to completion. PAUSED 2026-07-23 before any run: the D043 head-distribution defect was found first, and any horizon ladder measured under it would be confounded. No checkpoint or evaluation exists for this row. First suspect, when resumed, is that a horizon shorter than the `gamma=0.997` effective discount horizon starves the value head of the long-run return that makes balancing worth anything |
| D041 | REJECTED | Select the `M-JEPA` self-prediction rollout length `jepa_jumps` on the reserved development seeds over a pre-declared ladder of 5/8/11, instead of inheriting `T-JEPA`'s 5 unexamined. 11 is the ceiling imposed by the existing `jepa_jumps < sequence_length` validation at `sequence_length=12` | The mechanism is unchanged and remains the SPR `do_spr_loss` `jumps` of D034, faithful to `mila-iqia/spr` `0b9dd4e7`; only the number of autoregressive steps the world is trained on moves. Anti-collapse, predictor, EMA target, heads, terminal weighting, actor/value and the sealed protocol are untouched | D034 fixed a train/deploy mismatch by training multi-step at all; the same mismatch persists at a different scale, because the world is trained on 5 autoregressive steps and the actor then imagines 32, leaving drift beyond step 5 unconstrained. D040 measured that drift as the residual cause of the gate-1 shortfall, and D038 establishes dev-seed selection as the protocol for a quantity inherited unexamined from another arm. This is a documented final-measure divergence from the held-fixed `jumps=5` | Selection is on development seeds only; the sealed tiers were not consulted, and the ladder ran to completion rather than stopping on a favourable rung. The D040 drift mechanism is CONFIRMED and the fix does not help the actor. jumps 5: dev cosine 0.675, BC accuracy 0.9105, imagined continuation 0.444, imagined return 6.25, dev actor 474.43 vs BC 445.10, delta +29.33 [0.5,64.3], gate-2 gap 6.52. jumps 8: the drift fix works exactly as predicted — imagined continuation rises 0.444 -> 0.978 and imagined return 6.25 -> 68.54, world quality improves (dev cosine 0.721, JEPA loss 0.187), BC accuracy improves to 0.9134, and gate-2 divergence becomes the best in the project at 23.07 (BC 33.8 versus anti-BC 10.7, a 3.15x separation) — but the actor COLLAPSES to 245.87 against BC 368.40, delta -122.53 [-180.3,-61.1], because a long and now-survivable imagined horizon lets it exploit the model's residual long-horizon error. jumps 11: world training destabilizes outright (dev cosine 0.556, JEPA loss 0.339, BC accuracy 0.809, dev BC executing at 50.03). The ladder therefore selects 5 and the original inherited value stands. The finding is a genuine tradeoff rather than a tuning failure: too short a trained rollout starves the actor of signal, a matched one feeds it signal it can exploit, and too long a one breaks the world. This is consistent with the compounding-error regime of `2605.06732` (On Training in Imagination). CONFOUND DISCLOSED 2026-07-23: the entire ladder ran under the D043 head-distribution defect, with reward/continuation heads trained on real encoder latents but read on predictor-generated ones. Changing `jumps` changes how far the rollout leaves the real manifold and therefore changes the size of that mismatch, so the ladder varied two things at once. The j8 and j11 outcomes are retained as evidence and the rejection of 8 and 11 stands on them, but residual rollout error is NOT established as the sole cause and this row does not claim it |
| D040 | REJECTED | Hypothesis, tested and refuted: that D035's terminal weight (`jepa_terminal_weight=8`), inherited unexamined from `T-JEPA`, miscalibrates the `M-JEPA` continuation head and truncates the actor's imagined horizon, and that a development-selected per-arm weight would restore it | No code changed; the hypothesis was measured before any retraining | `M-JEPA` imagines continuation 0.284 over 32 steps while its BC really survives ~227 steps, which looks like gross over-prediction of termination and would have justified a per-arm weight ladder on the D038 pattern | Refuted on held-out real states: `M-JEPA` predicts P(continue)=0.9946 against an empirical 0.9896 (error +0.0050, implied episode 186 steps vs true 96), and is BETTER calibrated than `T-JEPA` (0.9994 vs 0.9896, error +0.0097, implied 1552 steps). The head is therefore correct on the real manifold and no weight change is warranted; the 0.284 arises on IMAGINED states. Combined with the D037 fidelity curves (imagined NRMSE 1.40->2.54 versus `T-JEPA`'s 0.70->1.59, state variance contracting 0.213->0.036), the mechanism is multi-step rollout drift: imagined latents leave the real manifold, the correctly-calibrated head reads them as terminal, and the actor's effective imagined horizon collapses to roughly 8 of 32 steps. Reducing that drift is a change to the predictor and training, i.e. the held-fixed architecture, so it is out of scope for this arm |
| D039 | SCREENED | Add an optional `max_episode_steps` override to `CartPolePixels`, `_run_control_episode` and the actor evaluator, defaulting to `None` so the pinned CartPole-v1 registration (500) is byte-unchanged unless a caller asks otherwise. Used only by `reviews/artifacts/d4_ceiling_diagnostic.py` on development seeds; no reported gate was measured with it | Dynamics, reward, termination and the RGB renderer are untouched; only the `TimeLimit` truncation wrapper is overridable, and only when explicitly requested | D011 pins the time limit, so lifting it needs a registered row. It was needed to answer whether the D037 gate-1 failure is a saturated benchmark or a policy that is simply not better | Diagnostic on 30 development seeds: at the pinned cap 500 the actor beats BC by +29.33 [0.17,64.27] with BC at the cap on 17/30 seeds; at cap 1500 the advantage QUADRUPLES to +122.33 (6.6% -> 15.5% relative) with BC at the cap on only 3/30. The actor is therefore genuinely better and the cap was compressing the measurement. But the interval WIDENS to [-54.97,300.90]: episode-return variance grows faster than the mean, so lifting the cap confirms the effect without rescuing significance. Raising the limit is consequently NOT a route to gate 1 and no sealed tier was re-run under it |
| D038 | SCREENED | Select the `M-JEPA` imagination-actor budget on the reserved development seeds `970000:970030` over a pre-declared ladder of 250/500/1000/1500 updates, instead of inheriting `T-JEPA`'s 500 unexamined | Actor-from-BC initialization, frozen world/prior/heads, PMPO equation 11, TD-lambda value, reverse-KL `beta`, one rollout per context, and direct greedy execution are unchanged; only the update count is selected | D019 and D023 establish that the actor budget is a per-arm quantity chosen on development seeds and then confirmed once on a sealed tier; carrying another arm's budget over is not that protocol | Development deltas: 250 gives -22.53 [-66.90,15.67], **500 gives +29.33 [0.47,64.30]**, 1000 gives +9.70 [-22.97,47.97], 1500 gives -1.20 [-37.10,39.27]. The ladder selects 500, which is the checkpoint already evaluated, so the sealed numbers stand unchanged. Disclosure: the 500-update sealed tiers were run before the ladder, so the ladder confirms rather than chooses; no checkpoint was selected using sealed data |
| D037 | SCREENED | Combined `M-JEPA` arm: replace only the dynamics `TimeSelfAttention` modules of the screened `T-JEPA` actor-critic with official Mamba-2 modules, at the D022 state expansion (`d_state=64`, `headdim=64`, `expand=1`, `d_conv=4`). `cartpole_jepa_config("mamba2")` carries those four values explicitly because the `D4LiteConfig` defaults are still the parameter-matched `d_state=16, headdim=32` of the rejected D021; `train_jepa_world` refuses to start a mamba2 run at the D021 values | Online encoder, SPR/BYOL EMA anti-collapse (D031), multi-step `jumps=5` (D034), terminal weighting (D035), deterministic predictor-as-rollout (D030), reward/continuation heads, actor/value, BC prior, pixel adapter, schedules, and the sealed protocol are held identical to `T-JEPA`; the temporal operator is the single moved axis | This is the combined thesis architecture (Mamba temporal operator plus non-generative JEPA), never previously run: every prior JEPA world is `T-JEPA`. Both ingredients are independently screened — Mamba at D022/D023 and non-generative JEPA at D030-D035 | Construction verified before training: 2 `MambaTimeMixer` modules installed and 0 `TimeSelfAttention` left in dynamics, EMA target encoder present, JEPA loss finite with gradient flowing to both the online encoder and the Mamba parameters, and the non-generative rollout runs. The D005 shortcut-prefix branching does not apply: `_sample_next_jepa` never uses `kv_cache`, so Mamba recurrent-cache semantics are not exercised by this arm. RESULTS. World: dev cosine 0.675 vs `T-JEPA` 0.648, final JEPA loss 0.236 vs 0.373, online std 0.0985 vs 0.0978 (no collapse). BC: held-out imitation accuracy 0.9105 vs 0.9077. Executed, paired on identical sealed seeds, `M-JEPA` beats `T-JEPA`: actor +41.61 [19.32,64.74] on 987 and +22.53 [-7.02,52.00] on 988; BC +61.90 [35.97,88.12] and +74.02 [42.20,106.02]. Imagined good-vs-bad divergence is larger and better shaped: gap 6.52 vs 4.52 with a monotone BC 16.41 > uniform 12.29 > anti-BC 9.89, and imagined continuation 0.284 rather than `T-JEPA`'s saturated 0.998, so this world finally models termination. The actor-versus-own-BC gate does NOT clear a strictly positive CI (987 +10.55 [-6.93,29.37], 988 +1.63 [-22.41,24.86]) and D038 shows the budget is not the cause. The cause is measured: `M-JEPA`'s BC reaches the CartPole-v1 500-step limit on 64/100 and 62/100 sealed seeds, and both policies sit at the cap — forcing the paired delta to exactly zero — on 50/100 and 52/100 seeds, so roughly half the sealed evidence carries no signal. Remaining suspect for the latent itself: the fidelity probe shows imagined across-batch state variance contracting with horizon (0.213 -> 0.036) and BC-versus-anti-BC separation decaying (0.536 -> 0.164), where `T-JEPA` stays flat (0.149 -> 0.178 and 0.430 -> 0.440); this is consistent with `M-JEPA`'s imagined episodes terminating early rather than with representational collapse, but it is not yet separated from it |
| D045 | REJECTED | Add `jepa_predictor_context` to widen what the action-conditioned predictor sees of the post-dynamics state: `"pooled_agent"` (default, unchanged: mean over agent tokens = 64) / `"concat_agent"` (128) / `"spatial_agent"` (spatial stream + agent tokens = 384). Hidden width scales with the context so the bottleneck does not simply move | Predictor placement, the deterministic predictor-as-rollout (D030), EMA anti-collapse (D031), multi-step jumps (D034), terminal weighting (D035), head routing (D043), encoder, dynamics, tokenizer and sampler are unchanged. The default reconstructs the original module exactly (`context_dim + d_model` reduces to `2*d_model`, same construction and init order), so every prior result is bit-unchanged | `CDPPredictor` mean-pools the agent tokens to ONE 64-d vector, so the entire self-prediction signal passes through 64 dimensions and cannot require the encoder to retain twelve independent inventory counts. Dreamer-CDP predicts from an 8192-d deterministic state with a PER-TOKEN cosine (`axis=-1`). CORRECTED 2026-07-29: this row cited `deter: int = 4096`, which is the `rssm.py:19` DATACLASS DEFAULT; the configured agent uses `deter: 8192` (`dreamerv3/configs.yaml:93`), so the divergence from our 64-d channel is 128x, not 64x, so the pooled 64-d channel is a local narrowing, not the source contract. This is the mechanism candidate isolated by the 2026-07-28 2x2, in which self-prediction alone under a clean sampler and with no task heads still eroded inventory 0.618 -> 0.474 | Suite 108 + 2 new tests pass and the default path is untouched; the rollout cache carries only the agent token, so `spatial_agent` redoes the context pass rather than reusing it (numerically identical, one extra dynamics pass). First suspect if it fails: the SPR projection is ALSO a global 64-d map (`jepa_projection_dim=64`), so widening the predictor alone may leave the loss-side bottleneck binding. REJECTED ON EFFICACY 2026-07-28, implementation retained as evidence and as an opt-in flag (default unchanged, so it costs nothing). Paired 2,500-update cells, inventory mean over 6 targets from a shared init of 0.618: isolated cell (tf=0.0, self-prediction only) 0.474 pooled -> **0.477** widened; baseline cell (tf=0.5, all losses) 0.404 -> 0.439. The isolated cell is the one that tests the mechanism and it moves by +0.003. The baseline cell's +0.035 is NOT claimed as an effect: two nominally identical runs (stage G and i1) gave 0.375 and 0.404, a spread of 0.029, so with one run per cell it is not separable from run-to-run variance. The change works mechanically -- dev cosine rises 0.548 -> 0.608 in the baseline cell, so the predictor does exploit the wider context. It simply does not reduce erosion. This is the pre-registered first suspect: the SPR loss projects through `JepaProjector` to a GLOBAL 64-d vector (`jepa_projection_dim=64`), so the encoder's gradient remains a 64-d global comparison however wide the predictor input is. Widening one side of a two-sided bottleneck changed nothing, which leaves the loss-side projection as the live candidate |
| D046 | SCREENED | Build the Mamba-2 substitution LAST in `D4LiteWorld.__init__`, after the objective branch, and reseed in `train_craftax_bc` / `train_craftax_imagination` before constructing the BC and value heads | Module set, shapes, forward paths, and every hyperparameter are unchanged; only the ORDER of construction and the seeding of two heads move. `state_dict` keys and shapes are identical either way (243 tensors for mamba2), so loading any existing checkpoint is numerically equivalent | DEFECT, found 2026-07-29 by an independent audit and verified here. `replace_dynamics_time_attention` ran BEFORE `_build_jepa`, so it consumed RNG and every `mamba2` world drew DIFFERENT weights for the JEPA predictor and all three projection MLPs than the `transformer` world at the same seed: **16 shared tensors differed at initialization** (encoder bit-identical, 62 tensors). Separately, neither Craftax head runner reseeded, so BC and value heads inherited whatever global RNG state the preceding world phase left -- which also differs by backend. Together these void D037's "the temporal operator is the single moved axis" contract for every T-vs-M comparison, on Craftax AND on CartPole (`train_jepa_world` seeds then builds, so the same construction order applies there) | After the fix: 227 shared tensors, 0 mismatched, and every backend-specific key contains `.time.`. Two regressions pin both properties (`test_backend_swap_does_not_perturb_shared_initialization`, `test_craftax_heads_are_seeded_independently_of_the_backend`); suite 113 + 3 = 116 pass. The magnitude of the confound on any past result is UNKNOWN because no initial head states were saved, so D037's Craftax and CartPole T-vs-M deltas are retrospectively uninterpretable as single-axis measurements rather than known-wrong |
| D036 | REJECTED | Swap the JEPA anti-collapse from SPR/BYOL EMA-target self-prediction (D031) to LeJEPA/SIGReg (`jepa_anticollapse="sigreg"`), faithful to LeJEPA Algorithm 2 (arXiv:2511.08544): drop the EMA target encoder, the teacher-student, and the stop-gradient. Two terms only: (i) invariance/prediction = the raw squared error between the predictor's next embedding and the actual next embedding, no L2 normalization and no stop-gradient; (ii) SIGReg = the Epps-Pulley sliced-normality statistic (`SlicingUnivariateTest`, 1024 slices) imported unchanged from `rbalestr-lab/lejepa` `c293d291`, digest-verified. Both act on the projector output, following the ViT reference path (`projector_arch="MLP"`); the ResNet `a_emb = g_emb` no-projector path does not apply to our transformer encoder. Combined with the reference convex combination `(1-lambda)*sim + lambda*sigreg`, `lambda=0.05` | The deterministic action-conditioned next-embedding predictor (the rollout), online encoder, reward/continuation heads (incl. terminal weighting D035), multi-step jumps (D034), actor/value, BC prior, pixel adapter, temporal operator, schedules, and the sealed protocol are all held identical to the EMA arm; anti-collapse is the single moved axis | Head-to-head test of the anti-collapse mechanism as the only attributable variable | REJECTED ON CARTPOLE, implementation retained and deferred, not used by any other arm. SIGReg behaves correctly in isolation (low for isotropic-Gaussian embeddings, high for collapsed ones). Evidence, with the configuration each number came from stated explicitly: the faithful recipe above (no stop-gradient, raw squared error) was screened to BC imitation accuracy 0.72 against EMA's 0.90, and was not carried to a sealed run; the sealed-tier failures (987: -26.22 [-64.10, 11.93]; 988: -9.43 [-50.91, 32.12]) were measured on the now-removed stop-gradient plus normalized-cosine variant, which reached BC accuracy 0.91. Both directions therefore fail: the faithful form degrades the control representation, and the variant that restores the representation still loses the imagination gate. Suspected cause is CartPole's very low intrinsic dimension against LeJEPA's ambient isotropic-Gaussian prior, not an implementation fault: Sub-JEPA (2605.09241) and SD-JEPA (2605.31111) document exactly this ambient-isotropy/intrinsic-geometry mismatch and relocate SIGReg into orthogonal subspaces to fix it, while LeWM (2603.19312) reaches 84.33% on low-dimensional Two-Room, so SIGReg world models are viable at higher effective dimension. Re-open only on a task of higher intrinsic dimension, where LeWM's published number provides an external correctness target that CartPole cannot |

`D021`, `D022` and `D023` are referenced by D037 and D038 but have NO rows in
this file. They were CartPole-era Mamba rows (the rejected parameter-matched
`d_state=16, headdim=32`; the selected state expansion `d_state=64,
headdim=64`; and the actor-budget protocol) that were lost when the ledger was
re-ordered. The values they pinned survive inline in D037, so no result depends
on recovering them, but the citations are dangling and must not be read as
evidence. Flagged 2026-07-29; not reconstructed, because inventing rows after
the fact would be worse than an honest gap.

`D010` is an administrative ledger correction made after the first preflight:
the port boundary was already disclosed in `README.md`, `SOURCE_MANIFEST.md`,
and source docstrings before execution, but it did not have its own numbered
row. No code or result was changed when the row was added.

`D012` records a real correction to that port. The flow equation was faithful,
but the first local composition trained task heads on a separate clean-latent
forward. Upstream MMBench2 trains them on the agent tokens returned by its
noised flow forward. All working CartPole checkpoints use the corrected route;
the older Crafter checkpoints remain historical evidence under their recorded
implementation hashes.

`D036` was reduced to the faithful LeJEPA recipe on 2026-07-23. Four local
variants explored during that run are removed from the code, because each was
introduced to move a gate number rather than to match a pinned source:

- `D036a` "pred-SIGReg" (`jepa_sigreg_pred_fraction=rho`), applying SIGReg to
  the predictor's own imagined rollout embeddings. Not in LeJEPA. It was tuned
  against the divergence probe (`rho=0` too contractive, `rho=0.5` too
  unstable), and because the EMA arm has no equivalent term it also broke the
  single-moved-axis contract that `D036` exists to test.
- Stop-gradient plus L2-normalized cosine on the invariance term. This is the
  SPR/BYOL predictor mechanism that LeJEPA explicitly removes, so adopting it
  re-imports the compared arm's own anti-collapse and voids the comparison.
- An additive `sim + lambda*sigreg` objective, replaced by the reference
  convex combination.
- An inverse-dynamics auxiliary head (proposed as `D037`, weight defaulted to
  0). It was never registered before training and never trained with, so no
  row is retained; it is a candidate mechanism for a future arm, not a fix.

One measurement from that run is worth keeping even though the mechanism was
removed: without pred-SIGReg the deterministic predictor's imagined rollout
appeared contractive, with action-0 and action-1 trajectories re-merging to
cosine 0.99 by step 16. That observation is recorded as unverified. It was
measured only as a latent cosine, which cannot separate "the latent collapses
toward an equilibrium" from "the batch stays diverse but each rollout loses
action-conditioned movement". The latent-fidelity probe planned for the M-JEPA
arm is the instrument that settles it.

## Transition convention

All local adapters use:

```text
(observation_t, action_t) -> (observation_{t+1}, reward_{t+1}, continue_{t+1})
```

Within the block-causal sequence, the action token stored at state position
`t+1` is `action_t`, the action that led to that state. Position zero receives
the dedicated start action. Reward and continuation head zero predict the
transition outcome that led to their current state position.

## Forbidden silent changes

- No Mamba fallback to GRU or attention.
- No Mamba inside the tokenizer during the initial factorial.
- No removal of reconstruction in the initial CDP arm.
- No reward-bin, event-sampling, calibration, or planner-budget tuning that
  differs between factorial arms.
- No loading Transformer dynamics weights into the Mamba arm with
  `strict=False`.
- No claim of a Dreamer-4 PMPO agent until policy/value imagination is
  implemented and tested separately.

## Current first-suspect order

Rewritten 2026-07-29 for the Craftax expert-data line; the previous list was
CartPole/Crafter-era and no longer described anything under test.

1. Unanchored full-LR encoder training. Every pinned reference anchors the
   encoder (V-JEPA 2-AC and Dreamer 4 freeze a pretrained one; Dreamer-CDP
   separates `enc_lr` 6e-6 from `dyn_lr` 4e-4; SPR anchors with Q-learning and
   reward). `enc_lr=6e-6` removes essentially all representation erosion.
2. Why the preserved representation converts to executed control for `T-JEPA`
   but not `M-JEPA`. This is the open question, and D046 means no existing
   T-vs-M comparison measured a single axis.
3. Terminal-sampler x task-head distortion: 16.67% of MTP continuation targets
   are terminals against 0.0105% in the replay (955x), 61.5% of the BCE mass,
   and the mean reward LABEL flips to -0.0238. Confirmed, ~23% of erosion.
4. Loss-side global 64-d projection (`jepa_projection_dim`). The predictor-input
   half of this was tested and rejected (D045); the loss half is untested.
5. `WorldLossNormalizer` omits the JEPA term while normalizing reward and
   continuation, so the three terms are not on a common scale.
6. Imagination context is truncated to 8 states per step
   (`imagination_actor_critic.py:517`) and `_sample_next_jepa` never uses a
   recurrent cache, so Mamba's long-memory thesis is not exercised over the
   32-step horizon. Shared by both arms, so not an M-specific explanation.
